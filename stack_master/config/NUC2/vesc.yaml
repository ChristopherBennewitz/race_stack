/**:
  # general parameters shared by all vesc-related nodes
  ros__parameters:
    # erpm (electrical rpm) = speed_to_erpm_gain * speed (meters / second) + speed_to_erpm_offset
    speed_to_erpm_gain: 4352.0
    speed_to_erpm_offset: 220.0

    # servo value (0 to 1) =  steering_angle_to_servo_gain * steering angle (radians) + steering_angle_to_servo_offset
    # Includes the former x1.3 steering correction from ackermann_mux.
    steering_angle_to_servo_gain: -1.43
    steering_angle_to_servo_offset: 0.505

    port: /dev/sensors/vesc
    duty_cycle_min: 0.0
    duty_cycle_max: 0.0
    current_min: 0.0
    current_max: 100.0
    brake_min: -20000.0
    brake_max: 200000.0
    speed_min: -69000.0
    speed_max: 69000.0
    position_min: 0.0
    position_max: 0.0
    servo_min: 0.00
    servo_max: 1.00

ackermann_to_vesc_node:
  ros__parameters:
    motor_output_topic: commands/motor/unsmoothed_speed
    servo_output_topic: commands/servo/unsmoothed_position

vesc_to_odom_node:
  ros__parameters:
    odom_frame: odom
    base_frame: base_link
    # publish odom to base link tf
    publish_tf: false
    use_servo_cmd_to_calc_angular_velocity: true
    # car wheelbase is about 25cm
    wheelbase: .321

throttle_interpolator:
  ros__parameters:
    rpm_input_topic: commands/motor/unsmoothed_speed
    rpm_output_topic: commands/motor/speed
    servo_input_topic: commands/servo/unsmoothed_position
    servo_output_topic: commands/servo/position

    # servo smoother - limits acceleration and smooths anything above limit
    max_acceleration: 5.0 # meters/second^2
    throttle_smoother_rate: 75.0 # messages/sec
    # servo smoother - limits rotation speed and smooths anything above limit
    # Preserves the previous servo slew rate after folding in the x1.3 gain.
    max_servo_speed: 5.0 # radians/second
    servo_smoother_rate: 75.0 # messages/sec
